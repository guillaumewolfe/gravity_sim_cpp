#include "engine/CreationTools/StateCreator.h"
#include "engine/CreationTools/CreatorManager.h"

StateCreator::StateCreator(RenderContext* renderContext, CreatorManager* manager)
    : m_renderContext(renderContext), m_manager(manager) {
    // Autre code d'initialisation si nécessaire
}